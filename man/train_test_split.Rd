% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_utils.R
\name{train_test_split}
\alias{train_test_split}
\title{Split Data into Training and Testing Sets}
\usage{
train_test_split(X, y, train_prop = 0.8, seed = 42)
}
\arguments{
\item{X}{Feature matrix}

\item{y}{Binary response vector}

\item{train_prop}{Proportion of data for training (default: 0.8)}

\item{seed}{Random seed for reproducibility (default: 42)}
}
\value{
List containing X_train, X_test, y_train, y_test
}
\description{
Creates stratified train/test splits for binary classification problems,
ensuring balanced class representation in both sets.
}
\examples{
\dontrun{
# Generate example data
X <- matrix(rnorm(100 * 10), 100, 10)
y <- rbinom(100, 1, 0.5)

# Create train/test split
split_data <- train_test_split(X, y, train_prop = 0.7)

# Access the splits
X_train <- split_data$X_train
y_train <- split_data$y_train
X_test <- split_data$X_test
y_test <- split_data$y_test
}
}
